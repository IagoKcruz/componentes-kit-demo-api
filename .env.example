DATABASE_URL=sqlite:///./agendamentos.db
DEBUG=false

# Autenticação — false = todos os endpoints livres, true = exige Bearer token JWT
AUTH_HABILITADO=true
JWT_SECRET=troque-esta-chave-em-producao
JWT_ALGORITMO=HS256
JWT_EXPIRACAO_MINUTOS=60
